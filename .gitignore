# Pi is no longer used; ignore the whole directory if a tool recreates it.
.pi/

# Herdr runtime state
herdr/*.log
herdr/*.sock
herdr/session.json
herdr/.plugins.lock
herdr/plugins/
herdr/plugins.json

# Miscellaneous local backups
*.bak
